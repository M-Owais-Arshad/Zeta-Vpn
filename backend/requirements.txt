# ZetaVPN panel — Python dependencies
# Pinned to versions verified to install cleanly (prebuilt wheels, no
# from-source compiles) on Python 3.11 through 3.14. Installed into a venv by
# install.sh. Bump deliberately, not automatically — re-run the smoke test
# (backend/test_smoke.py) after any version change.

fastapi==0.139.0
uvicorn[standard]==0.51.0
SQLAlchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2
PyJWT==2.13.0
bcrypt==5.0.0
pyotp==2.10.0
qrcode[pil]==8.2
psutil==7.2.2
httpx==0.28.1
PyYAML==6.0.3
